version: 0.2

phases:
  install:
    commands:
      # - printenv
      - npm install -g yarn
      - yarn install
  pre_build:
    commands:
      - yarn test
  build:
    commands:
      - yarn build
      # - publish to NPM

