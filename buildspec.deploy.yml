version: 0.1

phases:
  install:
    commands:
      - npm install -g yarn
      - yarn install
      - apt-get update
      - apt-get install jq
  pre_build:
    commands:
      - yarn docs
  build:
    commands:
      - yarn build
      - VERSION=$(node -e "console.log(require('./package.json').version)")
      - aws s3 rm --recursive s3://dragonchain-sdk-node-docs/latest/*
      - aws s3 cp --recursive ./docs s3://dragonchain-sdk-node-docs/latest
      - aws s3 rm --recursive s3://dragonchain-sdk-node-docs/$VERSION/*
      - aws s3 cp --recursive ./docs s3://dragonchain-sdk-node-docs/$VERSION/
      - export NPM_TOKEN=$(aws secretsmanager get-secret-value --secret-id npm/publish/token | jq -r '.SecretString' | jq -r '.NPM_TOKEN')
      - npm publish --access restricted


